<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Allows ADB to exercise the real alarm delivery path in debug builds only. -->
    <application>
        <receiver
            android:name=".reminder.LessonStartNotificationAlarmReceiver"
            android:exported="true"
            tools:replace="android:exported" />
        <receiver
            android:name=".reminder.AdditionalLessonNotificationAlarmReceiver"
            android:exported="true"
            tools:replace="android:exported" />
        <receiver
            android:name=".reminder.FourNotificationTestSetupReceiver"
            android:exported="true" />
    </application>
</manifest>
